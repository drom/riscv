'use strict';

// const extractWeb = names => new Promise((resolve, reject) => {
//   // // if (typeof names === 'string') {
//   // //   names = names.split(/\s+/);
//   // // }
//   // const requests = names.map(name => fetch('https://raw.githubusercontent.com/riscv/riscv-opcodes/master/opcodes-' + name));
//   // const responses = await Promise.all(requests);
//   // const texts = await Promise.all(responses.map(r => r.text()));
//   // const res = {};
//   // names.map((name, idx) => {
//   //   res[name] = op2obj(texts[idx], name);
//   // });
//   // return res;
// // };
// });

// module.exports = extractWeb;
